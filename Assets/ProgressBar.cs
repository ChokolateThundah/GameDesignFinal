using UnityEngine;
using System.Collections;

public class ProgressBar : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log("Name of progress bar: " + this.name);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
